import { Routes } from '@angular/router';
import { MainAppComponent } from './components/main/main-app/main-app.component';

export const routes: Routes = [
    {
        path: '',
        component: MainAppComponent
    }
];
